cat > ~/teclo/codemagic.yaml << 'EOF'
workflows:
  android-assemble:
    name: Teclo Build
    scripts:
      - name: Build APK
        script: |
          cd $CM_BUILD_DIR
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
EOF
